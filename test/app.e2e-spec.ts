/**
 * E2E test placeholder (V1)
 *
 * Nota:
 * - Este archivo está listo para cuando implementes `src/main.ts` y expongas `/health`.
 * - Si vas a ejecutar e2e tests, instala `supertest`:
 *   npm i -D supertest @types/supertest
 */

describe('App (e2e)', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
